# self
group_base_id = me.ctidy
group_sub_id = minecraft.mod.forge

version_major = 0
version_minor = 0
version_patch = 1
version_phase = SNAPSHOT

archives_base_id = ctidy-demo

modid = ctidydemo
mod_display_name = CTidy Demo

author_id = ctidy
author_name = CTidy

# minecraft & forge
minecraft_version = 1.16.5
minecraft_drange = [1.16.5,1.17)

#mappings_channel=snapshot
#mappings_version=20201028-1.16.3
mappings_channel=official
mappings_version=1.16.5

forge_version = 1.16.5-36.1.0
fml_drange = [36,)
forge_drange = [36.1.0,)

# other
jei_artifact_id = jei-1.16.5
jei_version=7.7.0.106

# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false